#include <stdio.h>
int main() {
		/* définition d'une variable */
    unsigned char maVariable; 			
    /* initialisation d'une variable */ 
    maVariable = 65;          			 
    /* affichage de la variable 'maVariable' de type 'char' */
    printf("ma variable vaut '%c'\n", maVariable);
}
